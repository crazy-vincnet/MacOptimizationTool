## MacOptimizationTool v1.8.0 — 스캔이 전체 디스크를 훑지 않습니다

중복 파일 검사가 너무 오래 걸리던 문제를 실측 기반으로 해결한 릴리스입니다.
메뉴바 크래시와 업데이트 확인 결함도 함께 고쳤습니다.

---

### ⚡ 중복 파일 검사

| 항목 | v1.7.3 | v1.8.0 |
|---|---|---|
| 방문한 파일 | 606,495 | **36,533** |
| 열거 시간 | 30.3초 | **1.7초** |
| 해시 대상 후보 | 133,644 | **155** |

무엇이 달라졌나:

- **제외 폴더를 진입 전에 건너뜁니다.** 기존에는 `~/Library`·`node_modules` 안으로 전부 내려간 뒤 결과만 버렸습니다. 비용은 그대로 냈던 셈입니다.
- 제외 목록에 `node_modules`, `.git`, `DerivedData`, `Pods`, `Caches`, `.venv` 등을 추가했습니다. 개발 환경 홈에서는 이 폴더들이 전체 파일의 절반 이상을 차지합니다.
- 후보 최소 크기 기본값이 **1MB** 입니다 (설정에서 100KB / 1MB / 10MB 선택). 2KB 파일 100개를 지워도 회수량은 0.2MB 였습니다.
- 경로 비교를 경계 단위로 바꿨습니다. 이제 `~/Projects/Library` 같은 사용자 폴더가 `~/Library` 와 함께 제외되지 않습니다.

새 옵션:

- **스캔 범위 프리셋** — 다운로드 / 데스크탑 / 도큐먼트 / 홈 전체 / 직접 선택
- **검사 정밀도** — 빠른 검사(크기 + 앞·중간·뒤 8KB 샘플 해시) / 정밀 검사(전체 SHA-256 재검증)
- **해시 캐시** — 재스캔에서 이전 해시를 재사용합니다. 파일이 바뀌면 자동 무효화됩니다.

### ⚡ 대용량 파일 검사

시스템·홈 Library 트리 가지치기를 적용했습니다. **결과는 완전히 동일**하고 시간만 줄어듭니다 (606,520 → 414,739개, 32.9초 → 21.0초).

**개발 캐시 폴더 제외** 토글(기본 꺼짐)을 켜면 1.6초까지 줄어듭니다. 다만 `node_modules` 안의 100MB 넘는 바이너리가 결과에서 빠집니다 — 실제로 디스크를 차지하는 파일이라 숨기는 판단은 사용자에게 맡겼습니다.

---

### 🐛 수정

- **메뉴바 네트워크 속도 계산 크래시.** 인터페이스 전송량이 4 GiB 를 넘기면 32비트 카운터가 되돌아가는데, 그 값을 부호 없는 정수로 빼면서 앱이 종료됐습니다. 장시간 켜 둔 세션에서 발생했습니다.
- **오프라인에서도 "최신 버전입니다" 로 표시되던 문제.** 이전 응답이 캐시에서 반환되어 확인이 성공한 것처럼 처리됐습니다. 이제 캐시를 쓰지 않는 전용 세션을 사용하고, 연결 없음·타임아웃을 구분해 알려줍니다.
- **폴더 크기가 실제보다 크게 표시되던 문제.** 하드링크를 여러 번 더했습니다. 10MB 파일에 링크 3개면 40MB 로 보고했습니다.
- **폴더 크기 계산 중 스캔 취소가 먹지 않던 문제.**
- 업데이트 확인 결과가 설정 화면에 계속 남습니다. 알림 창을 놓쳐도 확인할 수 있습니다.

### 📌 용량 표시 기준

회수 가능 용량은 **로컬 디스크 점유** 기준입니다. 목록에 보이는 파일 크기는 **파일 원래 크기**입니다.
iCloud 에서 내려받지 않은 파일은 원래 크기가 있어도 로컬 점유가 0 이라, 지워도 공간이 회수되지 않습니다. 방치 다운로드 목록에서 이런 파일은 **클라우드 전용** 으로 표시됩니다.
