## MacOptimizationTool v1.6.0 — 보안 강화 및 품질 릴리스

전체 코드 감사에서 발견된 **보안 결함 3건과 버그 8건**을 수정하고, 프로젝트를 SwiftPM 구조로 재편해 **회귀 테스트 56개와 CI**를 도입했습니다.
이번 릴리스는 새 기능보다 **안전성·정확성·검증 가능성**에 집중했습니다.

> 기존 버전을 사용 중이시라면 업데이트를 권장합니다. 특히 앱 완전 삭제기와 인앱 업데이트를 사용하는 경우 보안 수정이 직접 적용됩니다.

---

### 🔐 보안 수정

**1. 관리자 권한 임의 명령 실행 취약점 (심각)**
관리자 권한으로 파일을 지울 때 경로를 셸 레이어만 이스케이프하고 AppleScript 문자열 레이어는 처리하지 않았습니다. 파일명에 큰따옴표나 백슬래시가 포함되면 스크립트 문자열을 벗어나 임의 명령이 관리자 권한으로 실행될 수 있었습니다.
→ 셸/AppleScript 이중 이스케이프를 적용하고, 제어 문자가 포함된 경로는 삭제를 거부합니다.

**2. 관리자 권한 삭제 범위 제한 (심각)**
기존에는 `/Library` 하위 전체가 관리자 권한 영구 삭제 대상이 될 수 있었고, 보호 트리 검사도 적용되지 않았습니다.
→ 잔여물이 실제로 존재하는 하위 트리(Application Support, Caches, Logs, Preferences, LaunchAgents, LaunchDaemons, PrivilegedHelperTools, Containers, Saved Application State, Internet Plug-Ins)의 **하위 항목만** 삭제 대상이 되며, 각 트리의 루트 자체는 어떤 경우에도 삭제되지 않습니다.

**3. 인앱 업데이트 무결성 검증 (심각)**
기존에는 내려받은 `.dmg`를 검증 없이 즉시 마운트했습니다.
→ HTTPS와 GitHub 배포 호스트 허용목록을 강제하고, 릴리스 자산의 SHA-256 다이제스트와 실제 파일 해시가 **일치할 때만** 마운트합니다. 검증할 수 없으면 자동 설치를 중단하고 릴리스 페이지를 엽니다.

---

### 🐛 버그 수정

| 증상 | 원인 |
|---|---|
| 시동 항목 삭제 시 앱이 멈춤 | 메인 스레드에서 삭제를 동기 호출 |
| CPU 사용률이 튐 | 메뉴바와 대시보드가 같은 스냅샷에 동시 접근 |
| 시스템 수치가 실제와 다름 | 조회 실패 시 CPU 5% / RAM 60% 가짜 값 반환 |
| 휴지통 정리 후에도 공간이 줄지 않음 | 휴지통 항목을 휴지통으로 다시 이동 |
| macOS 13에서 방치 다운로드 화면 오작동 | macOS 14 전용 API 사용 |
| `1.0.0-dev` 버전이 `1.0.0-de` 로 표시 | 문자열 내 모든 `v` 제거 |
| v1.10 이상 릴리스 시 버전 갱신 실패 | 한 자리 숫자를 가정한 정규식 |
| 코드 서명 간헐 실패 | iCloud 동기화 폴더의 확장 속성 재부착 |

또한 **프로세스 폭주 감시기**가 코드 전체 주석 처리로 동작하지 않던 상태를 복구했습니다.

---

### 🌐 다국어 완성

영어·중국어·일본어 사용자에게 한국어가 그대로 노출되던 화면을 전부 정리했습니다.
메뉴바 미니 대시보드, 각 스캔 진행 상태, 권한 온보딩 모달, 디스크 헬스, 용량 맵, 개인정보 정리, 방치 다운로드, 시동 항목, 업데이트 안내, 알림 문구가 대상입니다.
번역 키 159개를 4개 언어로 추가했으며, 언어별 키 집합과 포맷 지정자 일치를 테스트가 검증합니다.

---

### 🧱 개발 인프라

- **SwiftPM 전환** — `MacOptimizationCore`(로직) / `MacOptimizationTool`(앱) / 테스트 3개 타깃으로 분리. 증분 빌드 동작.
- **단위 테스트 56개** — 삭제 안전 규칙, 잔여물 매칭 오탐, 업데이트 검증, 번역 정합성, 텔레메트리.
- **GitHub Actions CI** — push/PR 마다 빌드 → 테스트 → 패키징 → 서명 검증.
- Swift 6 strict concurrency 위반 0건, 컴파일 경고 0건.
- 직접 배포용/App Store용 entitlements 분리, MIT LICENSE 파일 추가.

전체 감사 내역은 저장소의 [`CODE_REVIEW.md`](https://github.com/crazy-vincnet/MacOptimizationTool/blob/main/CODE_REVIEW.md) 에서 확인하실 수 있습니다.
