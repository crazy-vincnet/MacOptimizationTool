## MacOptimizationTool v1.7.3 — 인앱 업데이트가 화면에 보입니다

업데이트 버튼을 눌러도 **아무 변화가 없던 문제**를 수정한 패치 릴리스입니다.

---

### 🐛 무엇이 문제였나

`SettingsView` 가 업데이터 객체를 뷰 본문에서 직접 읽고 있어 SwiftUI 가 상태 변경을 추적하지 못했습니다.
다운로드가 시작돼도 뷰가 다시 그려지지 않아, **진행률 오버레이가 한 번도 나타나지 않았습니다.**

실제로는 다운로드 → SHA-256 무결성 검증 → 디스크 이미지 마운트가 모두 정상 수행되고 있었습니다.
화면에 반응이 없어서 동작하지 않는 것처럼 보였을 뿐입니다.

### ✅ 어떻게 바뀌었나

- 업데이터를 정상적으로 관찰하도록 수정 — 진행률 바와 상태 문구(`연결 중` → `12.4 MB / 48.2 MB` → `검증 중` → `마운트 중`)가 실시간으로 표시됩니다.
- 오버레이가 사라진 뒤에도 최종 결과를 앱 정보 카드에 남깁니다. 검증 실패·마운트 실패도 화면에서 확인할 수 있습니다.

---

### 📌 인앱 업데이트 동작 범위

인앱 업데이트는 **설치 이미지(.dmg)를 검증하고 마운트하는 단계까지** 수행합니다.
실행 중인 앱을 자동으로 교체하지는 않으므로, 열린 창에서 앱을 `/Applications` 로 드래그해 교체해 주세요.

---

### 함께 포함된 최근 변경

- **v1.7.2** — 업데이트 확인 실패(네트워크 오류, 요청 한도 초과 등)를 "최신 버전입니다" 로 잘못 보고하던 문제 수정
- **v1.7.0** — 프로세스 자원 폭주 감지 기본 꺼짐, 임계값 완화(CPU 250%·메모리 8GB·약 2분 지속), 설정에서 직접 조절
