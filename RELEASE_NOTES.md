## MacOptimizationTool v1.7.0 — 프로세스 감시, 이제 사용자가 정합니다

프로세스 자원 폭주 경고가 **너무 자주 뜨던 문제**를 해결한 릴리스입니다.
감지 기능은 이제 **기본 꺼짐**이며, 켤 경우에도 조건이 훨씬 널널합니다.

---

### ⚙️ 무엇이 바뀌었나

| 항목 | v1.6.1 | v1.7.0 |
|---|---|---|
| 기본 상태 | 켜짐 (첫 실행 시 자동) | **꺼짐** |
| CPU 임계값 | 85% | **250%** |
| 메모리 임계값 | 2.5 GB | **8 GB** |
| 스캔 주기 | 5초 | 30초 |
| 지속 조건 | 없음 (1회 관측 즉시 경고) | **연속 4회 ≈ 2분** |

기존 85% 기준은 `ps` 의 `%cpu` 가 **코어 합산 값**이라는 점을 반영하지 않은 값이었습니다.
멀티코어 맥에서 한 코어를 정상적으로 다 쓰는 작업(빌드, 압축, 영상 인코딩, 브라우저 탭)은 100% 를 쉽게 넘기므로, 폭주가 아닌 정상 작업이 계속 경고로 잡혔습니다.

이제는 **임계값을 연속으로 넘긴 상태가 약 2분 이어질 때만** 알립니다. 순간적으로 튀는 작업은 무시됩니다.

---

### ✨ 추가

**설정 > 프로세스 감시** 섹션이 생겼습니다.

- 자원 폭주 감지 켜기 / 끄기 (기본 꺼짐)
- CPU 임계값: 100 – 600%
- 메모리 임계값: 2 – 32 GB
- 지속 시간 조건: 1 – 10분

조절한 값은 저장되며, 감시를 끄면 백그라운드 프로세스 스캔 자체가 멈춥니다.
신규 UI 문자열은 한국어·영어·중국어·일본어 모두 번역되어 있습니다.

---

### 📦 설치 경험 개선

- DMG 설치 창에 배경 이미지와 아이콘 배치 레이아웃을 적용했습니다.
- 볼륨 이름에 버전을 포함해, Finder 가 이전 버전 볼륨의 창 상태를 재사용하며 레이아웃이 깨지던 문제를 없앴습니다.
- 앱 서명을 Apple 이 비권장하는 `--deep` 대신 명시적 entitlements 서명으로 변경했습니다.

---

### 🔄 업데이트 권장 대상

v1.6.0 / v1.6.1 에서 프로세스 경고 팝업이 자주 떠서 불편했다면 이번 버전으로 업데이트하세요.
v1.6.0 의 보안 수정(관리자 권한 임의 명령 실행 취약점, 삭제 범위 화이트리스트, 업데이트 무결성 검증)은 모두 포함되어 있습니다.
