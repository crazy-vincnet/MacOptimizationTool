## MacOptimizationTool v1.7.1 — 번역 정합성 패치

v1.7.0 의 CI 가 잡아낸 번역 사전 결함 1건을 수정한 패치 릴리스입니다.
**기능 변경은 없습니다.** v1.7.0 을 이미 설치하셨다면 급히 업데이트하실 필요는 없습니다.

---

### 🐛 수정

영어 CPU 임계값 설명문 `exceed 100% on multicore` 에서 `% o` 가 "공백 플래그 + `%o` 변환 지정자"로 파싱됐습니다.
그 결과 같은 키의 포맷 지정자 개수가 영어 1개 / 한국어 0개로 어긋났습니다.

이 키는 `String(format:)` 을 거치지 않아 실제 크래시 위험은 없었지만, 같은 표기가 포맷 문자열에 들어가면 잘못된 인자를 읽습니다.
`100 percent` 표기로 바꿔 우회했습니다.

---

### 📌 v1.7.0 을 아직 안 쓰신다면 이번 버전으로 받으세요

v1.7.0 의 프로세스 감시 개선이 모두 포함되어 있습니다.

| 항목 | v1.6.1 | v1.7.x |
|---|---|---|
| 기본 상태 | 켜짐 (첫 실행 시 자동) | **꺼짐** |
| CPU 임계값 | 85% | **250%** (코어 합산) |
| 메모리 임계값 | 2.5 GB | **8 GB** |
| 스캔 주기 | 5초 | 30초 |
| 지속 조건 | 없음 (1회 관측 즉시 경고) | **연속 4회 ≈ 2분** |

**설정 > 프로세스 감시** 에서 켜기/끄기, CPU 임계값(100–600%), 메모리 임계값(2–32GB), 지속 시간 조건(1–10분)을 직접 조절할 수 있습니다.
DMG 설치 창 배경·레이아웃 개선도 함께 들어 있습니다. 전체 내용은 [v1.7.0 릴리스 노트](https://github.com/crazy-vincnet/MacOptimizationTool/releases/tag/v1.7.0)를 참고하세요.
