## MacOptimizationTool v1.6.1 — 동시성 안전성 패치

v1.6.0 에서 도입한 CI 가 곧바로 잡아낸 **Swift 동시성 안전성 결함 3건**을 수정한 패치 릴리스입니다.
사용자에게 보이는 동작 변경은 없습니다. v1.6.0 을 이미 설치하셨다면 급히 업데이트하실 필요는 없습니다.

> **v1.6.0 을 아직 안 쓰신다면 이번 버전으로 받으세요.** v1.6.0 의 보안 수정(관리자 권한 임의 명령 실행 취약점, 삭제 범위 제한, 업데이트 무결성 검증)이 모두 포함되어 있습니다. 전체 내용은 [v1.6.0 릴리스 노트](https://github.com/crazy-vincnet/MacOptimizationTool/releases/tag/v1.6.0)를 참고하세요.

---

### 🐛 수정

| 위치 | 문제 |
|---|---|
| 앱 목록 스캔 | `NSImage` 를 담은 앱 정보 구조체를 백그라운드에서 메인 액터로 전달 |
| 알림 권한 조회 | 비Sendable `UNNotificationSettings` 를 액터 경계 너머로 전달 |
| 앱 완전 삭제기 | 백그라운드 클로저가 비Sendable 값을 통째로 캡처 |

수정 방식:
- 백그라운드 스캔은 Sendable 한 `AppMetadata` 만 돌려주고, 앱 아이콘은 메인 액터에서 부착합니다.
- 알림 권한은 완료 핸들러 안에서 상태 값만 추출해 넘깁니다.
- 삭제 관련 클로저는 URL·크기·이름·번들 식별자만 캡처합니다.

---

### 📝 왜 v1.6.0 에서 못 잡았나

세 결함 모두 개발 환경의 Swift 6.3 툴체인에서는 통과했고, CI 러너의 **Swift 6.1 SDK 에서만 오류로 잡혔습니다**.
`NSImage` 와 `UNNotificationSettings` 의 Sendable 표기가 SDK 버전에 따라 다르기 때문입니다.

v1.6.0 에서 GitHub Actions CI 를 도입한 첫날 바로 값을 한 사례라, 기록을 남겨 둡니다.
배포된 v1.6.0 바이너리 자체는 정상 동작하며, 이번 수정은 소스 레벨의 정확성 문제입니다.
