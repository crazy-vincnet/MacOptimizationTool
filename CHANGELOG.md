# 변경 이력 (Changelog)

이 프로젝트의 주요 변경 사항을 기록합니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르며, 버전은 [유의적 버전](https://semver.org/lang/ko/)을 사용합니다.

---

## [1.8.0] - 2026-07-27

스캔 속도를 실측 기반으로 손보고, 폴더 크기 계산의 정확성 결함을 고친 릴리스입니다.
메뉴바 크래시와 업데이트 확인 관련 결함도 함께 수정했습니다. 모든 수치는 개발 환경 홈 디렉터리(파일 92만 개)에서 측정한 값입니다.

### ⚡ 성능 (Performance)

- **중복 파일 검사가 전체 디스크를 훑지 않습니다.**
  - 제외 대상 디렉터리를 **진입 전에** 가지치기합니다(`skipDescendants`). 기존에는 `~/Library`·`node_modules` 안으로 전부 내려간 뒤 결과만 버려서, 열거·`stat` 비용을 그대로 냈습니다.
  - 제외 목록에 `node_modules`, `.git`, `DerivedData`, `Pods`, `Caches`, `.venv`, `.gradle` 등을 추가했습니다. 개발 환경 홈에서 이 트리들이 전체 파일의 절반 이상(48만 개)을 차지합니다.
  - 후보 최소 크기를 2KB → **1MB** 로 올렸고, 설정에서 100KB / 1MB / 10MB 중 선택할 수 있습니다.
  - **측정: 방문 606,495개 → 36,533개, 열거 30.3초 → 1.7초, 해시 후보 133,644개 → 155개.**
- **대용량 파일 검사도 같은 방식으로 가지치기합니다.** 시스템·홈 Library 트리 제외는 항상 적용되며, **결과는 완전히 동일합니다**(측정: 606,520개 → 414,739개, 32.9초 → 21.0초, 결과 30개 동일).
- 해시 병렬도를 16 고정에서 **CPU 코어 수 기반**으로 바꿨습니다. SHA-256 은 CPU 바운드라 코어 수를 넘기면 경쟁만 늘어납니다.

### ✨ 추가 (Added)

- **중복 검사 옵션**: 스캔 범위 프리셋(다운로드 / 데스크탑 / 도큐먼트 / 홈 전체 / 직접 선택), 최소 파일 크기, 검사 정밀도(빠른 / 정밀).
  - 빠른 검사는 크기 + 앞·중간·뒤 8KB 샘플 해시까지 비교합니다. 정밀 검사는 샘플이 일치한 후보를 전체 SHA-256 으로 재검증합니다.
- **해시 결과 캐시**: `경로 + 크기 + 수정 시각 + 해시 종류` 를 키로 디스크에 저장해 재스캔에서 재사용합니다. 파일이 바뀌면 키가 달라져 자동으로 무효화됩니다.
- **대용량 파일 검사에 "개발 캐시 폴더 제외" 토글** (기본 꺼짐). 켜면 36,533개 / 1.6초로 줄지만, `node_modules` 안의 100MB 넘는 바이너리 11개가 결과에서 빠집니다. 그 파일들도 실제로 디스크를 차지하므로 숨기는 판단은 사용자에게 맡겼습니다.
- **업데이트 확인 결과를 화면에 계속 표시합니다.** 확인 중 / 최신 버전 / 새 버전 있음(설치 버튼) / 확인 실패(사유 + 릴리스 페이지 열기) 를 설정 화면에 남깁니다. 알림 창을 닫아도 결과가 사라지지 않습니다.
- **방치 다운로드에 "클라우드 전용" 표시.** iCloud 에서 내려받지 않은 파일은 지워도 로컬 공간이 회수되지 않으므로, 배지와 안내 문구로 구분합니다.

### 🐛 수정 (Fixed)

- **메뉴바 네트워크 속도 계산이 앱을 종료시키던 크래시.** `getifaddrs` 의 `if_data.ifi_ibytes` 는 `u_int32_t` 라 인터페이스가 4 GiB 를 넘기면 카운터가 되돌아갑니다. 그 값을 `UInt64` 로 빼면서 언더플로가 발생해 프로세스가 트랩됐습니다(`MenuBarManager.swift:119`). 이제 카운터가 감소한 주기는 계산하지 않고 기준선만 다시 잡습니다.
- **업데이트 확인이 캐시된 응답으로 성공 처리되던 문제.** `URLSession.shared` 의 기본 캐시 정책 때문에, 네트워크가 끊긴 상태에서도 이전 응답이 200 으로 반환되어 "최신 버전입니다" 로 표시됐습니다. 이제 캐시를 쓰지도 남기지도 않는 전용 세션을 사용하고, 연결 없음·타임아웃을 구분해 알려줍니다.
- **하드링크를 여러 번 더해 폴더 크기를 과대 계산하던 문제.** 10MB 파일에 하드링크 3개가 있으면 40MB 로 보고했습니다. 이제 같은 실체는 한 번만 셉니다.
- **폴더 크기 계산 중에는 스캔 취소가 동작하지 않던 문제.** 항목마다 취소를 확인하고, 중간에 멈춘 결과는 불완전함을 표시합니다.
- 업데이트 알림이 표시되지 않을 수 있던 문제. 조상 뷰에 걸린 알림 수정자와 충돌하지 않는 최신 API 로 교체했습니다.

### 🧱 구조 (Internal)

- 폴더 크기 계산이 언인스톨러·디스크 정리·개인정보 정리·방치 다운로드에 **네 번 복제**돼 있었습니다. `DirectorySize` 로 통합했고, 네 복사본 모두 가지고 있던 하드링크 중복·취소 미지원 결함이 한 번에 사라졌습니다.
- 회수 용량은 **로컬 점유**(할당 크기, 하드링크 1회), 목록 표시는 **파일 크기**(논리 크기)로 분리했습니다. 두 값을 하나로 합칠 수 없기 때문입니다 — iCloud 동기화 데스크탑의 79MB `.psd` 는 `st_blocks` 가 0 이었습니다.
- 신규 Core 모듈: `ScanExclusion`, `DuplicateScanConfig`, `FileIdentity`, `HashCache`, `DirectorySize`, `NetworkThroughput`.
- 단위 테스트 29개 추가 (제외 규칙·경계 비교·캐시 무효화·하드링크 신원·샘플 해시·폴더 크기·카운터 랩어라운드).

---

## [1.7.3] - 2026-07-26

인앱 업데이트를 눌러도 화면에 아무 변화가 없던 문제를 수정한 패치 릴리스입니다.

### 🐛 수정 (Fixed)

- **다운로드 진행률과 상태 문구가 표시되지 않던 문제.** `SettingsView` 가 `SparkleUpdaterManager.shared` 를 뷰 본문에서 직접 읽고 있어 SwiftUI 가 `@Published` 변경을 추적하지 못했습니다. `isDownloading` 이 true 로 바뀌어도 뷰가 다시 평가되지 않아 진행률 오버레이가 한 번도 나타나지 않았습니다. 다운로드·SHA-256 검증·마운트는 정상 수행되고 있었지만 사용자 화면에는 아무 반응이 없어 "업데이트가 동작하지 않는다" 로 보였습니다.
  - 업데이터를 `@ObservedObject` 로 관찰하도록 변경했습니다.
  - 오버레이가 사라진 뒤에도 최종 결과 문구(검증 실패·마운트 실패 포함)를 앱 정보 카드에 남깁니다.

### 📝 알아두실 점

인앱 업데이트는 **설치 이미지(.dmg) 를 검증 후 마운트하는 단계까지** 수행합니다.
실행 중인 앱을 자동으로 교체하지는 않으므로, 마운트된 창에서 앱을 `/Applications` 로 드래그해 교체해 주세요.

---

## [1.7.2] - 2026-07-26

업데이트 확인이 실패했을 때 "최신 버전입니다" 로 잘못 보고하던 문제를 수정한 패치 릴리스입니다.

### 🐛 수정 (Fixed)

- **업데이트 확인 실패를 최신 버전으로 위장하던 문제.** 네트워크 없음, DNS 실패, 타임아웃, GitHub API 요청 한도 초과(비인증 IP 당 시간당 60회), 응답 파싱 실패가 **모두** 성공적으로 확인했는데 새 버전이 없는 경우와 동일한 결과로 처리됐습니다. 사용자는 최신 버전을 쓰는 중이라고 오해하고, 원인 파악도 불가능했습니다.
  - 이제 확인 실패는 별도의 "⚠️ 업데이트 확인 실패" 알림으로 표시되며, 구체적 사유(네트워크 오류 내용 / HTTP 상태 코드 / 요청 한도 초과 / 응답 해석 실패)와 릴리스 페이지 열기 버튼을 제공합니다.
  - 확인 실패 시 "최신 버전" 문구는 어떤 경우에도 표시되지 않습니다.
- 신규 UI 문자열 7키를 한국어·영어·중국어·일본어로 번역했습니다.

### 📝 참고

버전 비교 로직(`UpdateVerification`) 자체에는 결함이 없었습니다. 실제 릴리스 API 응답으로 v1.6.1 / v1.7.0 / v1.7.1 기준 비교를 검증했고 모두 정상이었습니다.
문제는 실패 경로가 성공 경로와 같은 결과를 내보내던 보고 계층이었습니다.

---

## [1.7.1] - 2026-07-26

v1.7.0 의 CI 가 잡아낸 번역 사전 정합성 결함을 수정한 패치 릴리스입니다. 기능 변경은 없습니다.

### 🐛 수정 (Fixed)

- **영어 CPU 임계값 설명문의 거짓 포맷 지정자.** `exceed 100% on multicore` 에서 `% o` 가 "공백 플래그 + `%o` 변환"으로 파싱되어, 영어 값은 포맷 지정자 1개, 한국어 값은 0개로 집계됐습니다. 해당 키는 `String(format:)` 을 거치지 않으므로 실제 크래시 위험은 없었지만, 같은 실수가 포맷 문자열에서 반복되면 잘못된 인자를 읽게 됩니다. `100 percent` 로 표기해 우회했습니다.

---

## [1.7.0] - 2026-07-26

프로세스 자원 폭주 감시기가 너무 자주 경고를 띄우던 문제를 잡고, 감시 동작을 사용자가 직접 제어할 수 있게 만든 릴리스입니다.
설치 경험(DMG) 도 함께 손봤습니다.

### ⚙️ 변경 (Changed)

- **프로세스 자원 폭주 감지가 기본 꺼짐으로 바뀌었습니다.** 원하는 사용자만 설정에서 켜서 씁니다. 기존에는 첫 실행 시 자동으로 켜져 있었습니다.
- **경고 조건을 대폭 완화했습니다.** 빌드·압축·브라우저 탭처럼 정상적으로 자원을 많이 쓰는 작업까지 경고 대상이 되던 문제를 해결했습니다.
  - CPU 임계값 85% → **250%** (`ps` 의 `%cpu` 는 코어 합산 값이라 멀티코어에서 100% 를 넘습니다. 기존 85% 는 한 코어를 다 쓰는 정상 작업도 전부 걸렸습니다.)
  - 메모리 임계값 2.5GB → **8GB**
  - 스캔 주기 5초 → **30초**
- **순간 스파이크는 무시합니다.** 임계값 초과가 연속 4회(약 2분) 관측될 때만 알립니다. 중간에 한 번이라도 임계값 아래로 내려오면 누적이 초기화됩니다.

### ✨ 추가 (Added)

- **설정 > 프로세스 감시** 섹션을 추가했습니다. 감시 켜기/끄기, CPU 임계값(100–600%), 메모리 임계값(2–32GB), 지속 시간 조건(1–10분)을 직접 조절할 수 있고 설정은 저장됩니다.
- 신규 UI 문자열 8키를 한국어·영어·중국어·일본어로 번역했습니다.

### 🐛 수정 (Fixed)

- 감시를 끄면 백그라운드 `ps` 스캔 자체가 멈춥니다. 기존에는 꺼진 상태에서도 5초마다 프로세스 목록을 뜨고 결과만 버렸습니다.
- 종료된 프로세스의 PID 가 알림 이력에 계속 쌓이던 문제를 수정했습니다.

### 📦 배포 (Packaging)

- DMG 설치 창에 배경 이미지와 아이콘 배치 레이아웃을 적용했습니다. 볼륨 이름에 버전이 포함되어 이전 버전 볼륨의 Finder 창 상태를 재사용하는 문제도 사라졌습니다.
- DMG 작업 디렉터리를 iCloud 동기화 폴더 밖(`TMPDIR`)으로 옮겼습니다. 동기화 폴더 안에서는 `fileprovider` 개입으로 Finder 뷰 설정 적용이 `-10006` 으로 실패했습니다.
- `.app` 서명을 `--deep` 대신 명시적 entitlements 서명으로 변경했습니다 (`--deep` 은 Apple 이 비권장).

---

## [1.6.1] - 2026-07-25

v1.6.0 도입 직후 CI 가 잡아낸 Swift 동시성 안전성 결함을 수정한 패치 릴리스입니다.
사용자에게 보이는 동작 변경은 없습니다.

### 🐛 수정 (Fixed)

- **앱 정보 구조체를 액터 경계 너머로 전달하던 문제.** `SelectedAppInfo` 는 `NSImage` 를 담고 있어 Sendable 이 아닌데, 백그라운드 스캔 결과를 그대로 메인 액터로 넘기고 있었습니다. 이제 백그라운드는 Sendable 한 `AppMetadata` 만 돌려주고, 아이콘은 메인 액터에서 부착합니다.
- **알림 권한 상태 조회.** `UNNotificationSettings` 는 Sendable 이 아니므로 비격리 컨텍스트에서 전달할 수 없습니다. 완료 핸들러 안에서 권한 상태 값만 추출해 넘기도록 변경했습니다.
- **언인스톨러 백그라운드 클로저의 캡처 범위.** 클로저가 `SelectedAppInfo` 전체를 캡처하고 있었습니다. 필요한 URL·크기·이름·번들 식별자만 캡처하도록 좁혔습니다.

### 📝 참고

세 결함 모두 로컬 Swift 6.3 툴체인은 통과했지만, CI 러너의 Swift 6.1 SDK 에서 오류로 잡혔습니다.
v1.6.0 에서 도입한 CI 가 첫날 바로 역할을 한 사례입니다. 배포된 v1.6.0 바이너리 자체는 정상 동작합니다.

---

## [1.6.0] - 2026-07-25

전체 코드 감사에서 발견된 보안 결함과 버그를 수정하고, 프로젝트를 SwiftPM 구조로 재편하여 회귀 테스트와 CI를 도입한 릴리스입니다.
기능 추가보다 **안전성·정확성·검증 가능성**에 집중했습니다. 자세한 감사 결과는 [`CODE_REVIEW.md`](CODE_REVIEW.md)를 참고하세요.

### 🔐 보안 (Security)

- **AppleScript 인젝션으로 인한 관리자 권한 임의 명령 실행 취약점 수정.**
  관리자 권한 삭제 시 경로를 셸 레이어만 이스케이프하고 AppleScript 문자열 레이어(`"`, `\`)는 처리하지 않아, 파일명에 큰따옴표나 백슬래시가 포함되면 `do shell script "..."` 문자열을 탈출할 수 있었습니다. 셸/AppleScript 이중 이스케이프를 적용하고, 제어 문자가 포함된 경로는 삭제 자체를 거부하도록 변경했습니다.
- **관리자 권한 삭제 범위를 화이트리스트로 제한.**
  기존에는 `/Library` 하위 전체가 root `rm -rf` 대상이 될 수 있었고 보호 트리 검사도 적용되지 않았습니다. 이제 잔여물이 실제로 존재하는 하위 트리(`Application Support`, `Caches`, `Logs`, `Preferences`, `LaunchAgents`, `LaunchDaemons`, `PrivilegedHelperTools`, `Containers`, `Saved Application State`, `Internet Plug-Ins`)의 **하위 항목만** 삭제 대상이 되며, 각 트리의 루트 자체는 어떤 경우에도 삭제되지 않습니다.
- **인앱 업데이트 무결성 검증 추가.**
  기존에는 내려받은 `.dmg`를 검증 없이 즉시 마운트했습니다. 이제 HTTPS와 GitHub 배포 호스트 허용목록을 강제하고, GitHub Release API가 제공하는 자산 다이제스트와 실제 파일의 SHA-256을 대조하여 **일치할 때만** 마운트합니다. 검증 해시가 없거나 불일치하면 자동 설치를 중단하고 릴리스 페이지를 엽니다.
- **릴리스 노트에 DMG SHA-256 체크섬 자동 게시.** 수동 대조가 가능합니다.
- 사용하지 않던 `appcast.xml`(v1.3.0에서 갱신 중단, 구버전 파일명 참조)을 제거했습니다.

### 🐛 수정 (Fixed)

- **시동 항목 삭제 시 UI 프리즈/데드락.** 메인 스레드에서 휴지통 이동을 동기 호출해 세마포어 대기와 관리자 인증 프롬프트가 UI를 막았습니다. 비동기 경로로 분리했습니다.
- **CPU 사용률 데이터 레이스.** 메뉴바 타이머와 대시보드 텔레메트리 스트림이 직전 CPU 틱 스냅샷에 동시 접근했습니다. 락으로 보호했습니다.
- **거짓 시스템 수치 표시 제거.** 커널 통계 조회 실패 시 CPU 5%, RAM 60%라는 가짜 값을 반환하고 있었습니다. 이제 측정 실패는 실패로 표시합니다(메뉴바는 `--`).
- **휴지통 카테고리 정리 시 공간이 회수되지 않던 문제.** `~/.Trash` 내부 항목을 다시 휴지통으로 옮기고 있었습니다. 이제 영구 삭제 경로로 처리합니다.
- **macOS 13에서 방치 다운로드 화면이 동작하지 않던 문제.** macOS 14 전용 API(`onChange(of:initial:_:)`)를 최소 지원 버전 선언(13.0)과 무관하게 사용하고 있었습니다. 버전 분기를 적용했습니다.
- **버전 문자열 파싱 오류.** `1.0.0-dev`가 `1.0.0-de`로 잘리던 문제를 수정했습니다(선행 `v`만 제거).
- **릴리스 스크립트의 버전 치환 실패.** 한 자리 숫자를 가정한 정규식 탓에 `v1.10.0` 이상에서 Info.plist 버전이 갱신되지 않았습니다. 세 자리 시맨틱 버전 전체를 지원하고, 형식 검증을 추가했습니다.
- **코드 서명 간헐 실패.** iCloud 동기화 폴더에서 `com.apple.FinderInfo`가 비동기로 다시 부착되어 서명이 거부되던 문제를 재시도 로직으로 해결했습니다.
- Swift 6 strict concurrency 위반 전량 해소(에러 4건 → 0건), 컴파일 경고 0건.

### 🌐 다국어 (Internationalization)

- **하드코딩되어 있던 한국어 UI 문자열 전량 번역화.** 영어·중국어·일본어 사용자에게 한국어가 그대로 노출되던 화면(메뉴바 미니 대시보드, 각 스캔 진행 상태, 권한 온보딩 모달, 디스크 헬스, 용량 맵, 개인정보 정리, 방치 다운로드, 시동 항목, 업데이트 안내, 알림 문구)을 모두 정리했습니다.
- 번역 키 159개를 4개 언어로 추가했으며, 언어별 키 집합과 포맷 지정자 개수 일치를 테스트로 검증합니다.

### ⚙️ 변경 (Changed)

- **프로세스 폭주 감시기 재활성화.** 코드가 전체 주석 처리되어 동작하지 않는 상태로 배포되고 있었습니다. `Task` 기반 5초 주기 감시로 복구했고, 판정 임계값과 안내 문구가 같은 상수를 공유하도록 정리했으며, 사용자 설정을 유지합니다.
- **entitlements 분리.** 직접 배포용(비샌드박스)과 Mac App Store 제출용(샌드박스)을 분리했습니다. 기존에는 샌드박스 entitlements가 서명에 적용되지 않아 선언과 실제 동작이 어긋나 있었습니다.
- `codesign --deep`(deprecated) 사용을 중단했습니다.
- 릴리스 스크립트의 `git add .` 무차별 스테이징을 명시적 스테이징으로 변경했습니다.
- 앱 내 버전 표기를 번들 정보에서 읽도록 변경해 릴리스마다 어긋나지 않게 했습니다.

### 🧱 개발 인프라 (Infrastructure)

- **SwiftPM 전환.** `swiftc` 단일 호출을 3개 타깃으로 재편했습니다.
  - `MacOptimizationCore` — 삭제 안전 규칙, 잔여물 매칭, 업데이트 검증, 텔레메트리, 번역
  - `MacOptimizationTool` — SwiftUI 앱 본체
  - `MacOptimizationCoreTests` — 단위 테스트
  세 타깃 모두 Swift 6 언어 모드로 고정했습니다. 증분 빌드가 동작합니다.
- **단위 테스트 56개 추가**(swift-testing). 삭제 안전 규칙, 잔여물 매칭 오탐, 업데이트 검증, 번역 정합성, 텔레메트리를 검증합니다.
- **GitHub Actions CI 추가.** push/PR마다 빌드 → 테스트 → 패키징 → 서명 검증을 실행합니다.
- `test.sh` 추가. Xcode 없이 Command Line Tools만 설치된 환경도 자동 대응합니다.
- MIT `LICENSE` 파일 추가(README 배지와 실물 불일치 해소).
- 미사용 코드 제거: 중복된 시동 항목 화면 한 쌍, `AppDelegate`의 사문화된 메뉴바 구현.

---

## [1.5.0] - 2026-07-25

- 인앱 DMG 재다운로드 옵션 추가
- 버전 비교 시 공백 및 `v` 접두사 처리 보강
- Swift 6 Sendable 클로저 경고 및 deprecated UI 핸들러 정리

## [1.4.0] - 2026-07-25

- FDA 전체 디스크 접근 권한 온보딩 모달
- 전 메뉴 비동기 스캔 취소
- 16-Worker 병렬 해시 엔진
- README 및 GitHub Pages 랜딩 사이트 갱신
